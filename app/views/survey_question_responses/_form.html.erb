<%= form_for(@survey_question_response) do |f| %>
  <% if @survey_question_response.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@survey_question_response.errors.count, "error") %> prohibited this survey_question_response from being saved:</h2>

      <ul>
      <% @survey_question_response.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :observation_id %><br>
    <%= f.text_field :observation_id %>
  </div>
  <div class="field">
    <%= f.label :survey_question_id %><br>
    <%= f.text_field :survey_question_id %>
  </div>
  <div class="field">
    <%= f.label :response %><br>
    <%= f.text_field :response %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
